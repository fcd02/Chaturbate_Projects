# Local model binaries

This directory is intentionally empty in the public Git repository.

The Mobile Reviewer may use local model binaries such as `nudenet_640m.onnx`, but large/runtime model binaries are excluded from the public Git mirror by the CTBRec Git Bridge. Keep the required binary in the live installation's `models/` directory.
